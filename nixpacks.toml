[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.bcmath", "php82Extensions.gd", "php82Extensions.xml", "php82Extensions.zip", "php82Extensions.curl", "composer", "bash"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader", "chmod +x railway_start.sh"]

[start]
cmd = "bash railway_start.sh"
